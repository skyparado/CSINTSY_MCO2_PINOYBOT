# PinoyBot dependencies -- install with:  pip install -r requirements.txt
#
# sklearn-crfsuite is required at PREDICTION time as well as training time:
# the saved model is a CRF, and unpickling it needs the library present.
scikit-learn>=1.3
sklearn-crfsuite>=0.5
pandas>=2.0
openpyxl>=3.1
